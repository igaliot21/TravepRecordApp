<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TravepRecordApp.ProfilePage">
    <ContentPage.Content>
        <StackLayout Margin="20,40,20,0">
            <Label x:Name="lblPostCount" HorizontalOptions="Center" Text="---"
                TextColor="{StaticResource blueColor}" FontAttributes="Bold"  FontSize="20"/>
            <Label VerticalOptions="Center" Text="Post" HorizontalOptions="Center"
                TextColor="DarkSlateGray" FontAttributes="Bold" FontSize="20" />
            <ListView x:Name="listviewCategories" Margin="0,20,0,10">
                <ListView.ItemTemplate>
                    <DataTemplate> 
                        <TextCell Text="{Binding Path=Key}" Detail="{Binding Path=Value}"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
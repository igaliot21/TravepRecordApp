<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TravepRecordApp.RegisterPage">
    <StackLayout x:Name="stackRegister" VerticalOptions="CenterAndExpand" Margin="15,0,15,0">
        <Entry x:Name="entUser" Placeholder="User Email" 
               Keyboard="Email" TextColor="{StaticResource blueColor}"
               Text="{Binding Email, Mode=TwoWay}"></Entry>
        <Entry x:Name="entPassword" Placeholder="User Password" 
               IsPassword="True" TextColor="{StaticResource blueColor}"
               Text="{Binding Password, Mode=TwoWay}"></Entry>
        <Entry x:Name="entConfirmPassword" Placeholder="Confirm Password" 
               IsPassword="True" TextColor="{StaticResource blueColor}"
               Text="{Binding ConfirmPassword, Mode=TwoWay}"></Entry>
        <Button x:Name="RegisterButton" Text="Register" 
                Command="{Binding registerCommand}"
                CommandParameter="{Binding User}"
                Margin="0,20,0,0"
                Style="{StaticResource normalButton}"></Button>
    </StackLayout>
</ContentPage> 